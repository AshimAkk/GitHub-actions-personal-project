name: GitHub Actions Udemy course
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting message
        run: echo 'Hello there, how are you'
      - name: Print second message
        run: echo "${{ toJson(github) }}"  //accessingthe github object usin
